---
# SPDX-FileCopyrightText: PoppyCake, s.r.o.
# SPDX-License-Identifier: GPL-3.0-or-later
title: "[daemon]"
description: Daemon-wide settings (shutdown_timeout, timezone, TLS, metrics, includes), plus the CLI flags and env vars for the config, data directory, and listen address.
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";

`[daemon]` holds settings for the whole daemon. The table is optional. A change
to any `[daemon]` key except `include` and `include_cron` needs
`runwisp restart`; [`runwisp reload`](/operations/reload/) rejects it.

The config path, data directory, and listen address are
[CLI flags](#cli-flags-config-data-directory--listen-address), because they are
needed before the config file is read.

## Keys

```toml
[daemon]
shutdown_timeout = "10s"
timezone         = "Europe/Bratislava"
external_url     = "https://runwisp.example.com"
check_updates    = true
tls              = "off"
tls_cert         = ""
tls_key          = ""
metrics_enabled  = false
metrics_listen   = ""
trusted_proxies  = ["127.0.0.1/32"]
include          = ["conf.d/*.toml"]
# include_cron   = ["/etc/crontab", "/etc/cron.d/*"]
```

### `shutdown_timeout`

**type**: `duration` **default**: `"10s"`

How long the daemon waits for running tasks and services to exit on shutdown.
After that, it SIGKILLs what is left.

- Each unit's [`graceful_stop`](/configuration/tasks/#graceful_stop) must fit
  inside this. If one is longer, the daemon warns about it by name at boot, and
  at shutdown the unit may be killed before its own grace window ends.
- To fix the warning, raise `shutdown_timeout` or lower that unit's
  `graceful_stop`.

```toml
[daemon]
shutdown_timeout = "30s"
```

### `timezone`

**type**: `string` **default**: the host zone (`UTC` if it can't be detected)

The IANA zone used to read [`cron`](/configuration/tasks/#cron) for every task
that doesn't set its own [`timezone`](/configuration/tasks/#timezone). An
unknown zone name is rejected at load.

```toml
[daemon]
timezone = "Europe/Bratislava"
```

### `external_url`

**type**: `string` **default**: unset

The public address of this daemon's Web UI, for example
`https://runwisp.example.com` behind a reverse proxy or `http://192.168.1.50:9477`
on a LAN. Notifications (Slack, Telegram, and others) add a "View run" link of
the form `<external_url>/tasks/<task>/<id>`.

- Must be `http` or `https` with a host. A trailing slash is removed.
- The daemon never connects to this URL.
- When unset, notifications leave out the link.

```toml
[daemon]
external_url = "https://runwisp.example.com"
```

### `check_updates`

**type**: `boolean` **default**: `true`

Checks for a newer release (usually every 6 hours) and shows an amber marker
next to the version in the Web UI and TUI. It never downloads or installs
anything. Set `false` to keep the daemon fully offline.

```toml
[daemon]
check_updates = false
```

### `tls`, `tls_cert`, `tls_key`

**tls**: `enum`, default `"off"` · **tls_cert**: `string`, default unset ·
**tls_key**: `string`, default unset

How the HTTP server does TLS. The bind address comes from `--host`.

| Setting                | Loopback bind (`127.0.0.1`, default) | Other bind (`0.0.0.0`, LAN IP) |
| ---------------------- | ------------------------------------ | ------------------------------ |
| `tls = "off"`          | HTTP                                 | HTTP, with a startup warning   |
| `tls = "auto"`         | HTTP                                 | HTTPS, self-signed certificate |
| `tls_cert` + `tls_key` | HTTPS, your certificate              | HTTPS, your certificate        |

```toml
[daemon]
tls = "auto"
```

- **Self-signed (`auto`):** generated on first boot and stored in `<data>/tls/`.
  The startup log prints its fingerprint
  (`Serving HTTPS bind=0.0.0.0 cert=self-signed fingerprint=sha256:…`) so you can
  compare it with what your browser shows. The CLI and TUI pin the certificate
  on first connect, like SSH.
- **New certificate:** if you delete `<data>/tls/` and restart, the CLI refuses
  to connect until you clear the old pin.
- **Your own certificate:** set `tls_cert` and `tls_key` to a PEM pair (from
  your CA or `mkcert`). Set both or neither. This overrides `tls`.
- **Reverse proxy:** leave `tls = "off"` when a proxy (nginx, Caddy, Traefik)
  handles TLS, and set [`trusted_proxies`](#trusted_proxies) so the daemon
  trusts `X-Forwarded-Proto` and marks cookies `Secure`.
- **No ACME / Let's Encrypt.** For a publicly trusted certificate, use a reverse
  proxy or your own pair.
- The `RUNWISP_TLS` env var overrides `tls`.

### `metrics_enabled`

**type**: `boolean` **default**: `false`

Turns on the OpenMetrics endpoint at `/metrics` (for Prometheus, Grafana Agent,
OpenTelemetry). It is off by default because the metrics show task names and the
daemon version. See [Metrics](/operations/metrics/) for the metric list.

```toml
[daemon]
metrics_enabled = true
```

### `metrics_listen`

**type**: `string` **default**: unset

A separate `host:port` for `/metrics`, for example `"127.0.0.1:9478"`. Use it to
keep metrics on loopback while the Web UI is public.

- When set, `/metrics` is served only on this address. The UI, REST API, and
  `/health` stay on the main listener.
- Setting it turns metrics on. `metrics_enabled = false` together with
  `metrics_listen` is an error.
- This listener is always plain HTTP. Keep it on loopback or a private
  interface.

```toml
[daemon]
metrics_listen = "127.0.0.1:9478"
```

### `trusted_proxies`

**type**: `string[]` (CIDR) **default**: unset

Reverse proxies (nginx, Caddy, Traefik, Cloudflare Tunnel) whose
`X-Forwarded-For` header RunWisp trusts for the real client IP.

- A bare IP means a single address (`"127.0.0.1"` is `/32`).
- `0.0.0.0/0` and `::/0` are rejected, because they would let any client fake
  its IP.
- The `RUNWISP_TRUSTED_PROXIES` env var (comma-separated) overrides this key.
- See [Reverse proxies](/operations/auth/#reverse-proxies).

```toml
[daemon]
trusted_proxies = ["127.0.0.1/32", "10.0.0.0/8"]
```

### `allow_station_dispatch`

**type**: `boolean` **default**: `false`

Lets a connected control-plane peer start **ad-hoc** runs: one-shot commands
that are not in your config. Without it, the peer can only watch runs and
trigger tasks defined in `runwisp.toml`.

- Ad-hoc runs never change your task definitions. They are logged and stored
  like any other run.
- They run with the daemon's privileges. Enable this only if you need it.

```toml
[daemon]
allow_station_dispatch = true
```

### `include`

**type**: `string[]` (glob) **default**: unset

Extra TOML files to merge into the config, so you can split tasks across files.

```toml
# runwisp.toml
[daemon]
include = ["conf.d/*.toml", "services/*.toml"]
```

```toml
# conf.d/backups.toml
[tasks.nightly-backup]
cron = "0 3 * * *"
run  = "/opt/backup.sh"
```

- Globs and relative paths inside a file resolve against that file's directory.
  `env_file = "backup.env"` in `conf.d/backups.toml` means `conf.d/backup.env`.
- Tasks, services, compose blocks, notifiers, and routes from all files are
  merged. The root file loads first, then matches in path order.
- Names must be unique across all files. A duplicate is an error that names both
  files.
- `[daemon]`, `[defaults]`, `[storage]`, and `[notify]` are allowed only in the
  root file.
- Includes don't nest: an included file can't have its own `include`.
- `runwisp reload` re-reads the globs, so added, changed, or deleted files are
  picked up without a restart.

### `include_cron`

**type**: `string[]` (glob) **default**: unset

Real crontabs to load as tasks. RunWisp reads these files and never writes them.
Every job becomes a task with captured output, run history, and a page in the UI.

```toml
[daemon]
include_cron = [
  "/etc/crontab",
  "/etc/cron.d/*",
  "/var/spool/cron/crontabs/*", # per-user crontabs (needs a root daemon)
]
```

- `sudo runwisp takeover` finds the crontabs on the host and writes this key for
  you. It doesn't edit an `include_cron` you already have; it prints what to add.
- After `crontab -e`, run [`runwisp reload`](/operations/reload/).
- `runwisp promote <task>` copies a job into `runwisp.toml` and comments out the
  crontab line. See
  [Graduating a job](/coming-from/crontabs/#graduating-a-job-into-your-own-config).

:::note[Held jobs]
While a system cron daemon is running, RunWisp **holds** the jobs it read: they
are listed with their schedule, but RunWisp doesn't start them, so nothing runs
twice. `sudo runwisp takeover` ends the hold. If cron is stopped some other way,
the hold ends within a minute. See [Held jobs](/coming-from/cron/).
:::

#### Which files, and who the jobs run as

The path decides the format:

| Path                              | Format            | Jobs run as                      |
| --------------------------------- | ----------------- | -------------------------------- |
| `/etc/crontab`, any `cron.d/*`    | system crontab    | the user in the line's 6th field |
| `/var/spool/cron/crontabs/<user>` | per-user spool    | `<user>`, from the file name     |
| `/var/spool/cron/<user>`          | per-user spool    | `<user>` (RHEL/SUSE layout)      |
| anything else                     | `crontab -l` dump | the daemon's user                |

- Running a job as another user needs a **root daemon**. Without root, RunWisp
  refuses that spool file instead of running the jobs as itself. Your own
  crontab works without root.
- A spool file must be owned by the user it is named after (cron checks the
  same).

#### Which files a glob matches

A glob picks only the files cron would read:

- **`/etc/crontab` and `cron.d/*`:** regular files with names made of letters,
  digits, `-`, and `_`. So `backup.dpkg-old`, `job.disabled`, and `README` are
  skipped.
- **Spool directories:** any name that could be a user (`john.doe` is fine),
  except `tmp.*` (the temporary file `crontab -e` writes).
- A path you name exactly is always read, whatever its name.
- Skipped files are reported with the reason.
- A spool directory the daemon can't read (Debian ships
  `/var/spool/cron/crontabs` as `1730 root:crontab`) is reported, because cron
  may be running jobs RunWisp can't see. Run RunWisp as root or as the owner.

#### Other rules

- **A job RunWisp can't reproduce is skipped; the rest of the file still
  runs.** Each skip is reported as `file:line` at boot and by `runwisp reload`,
  `runwisp status`, and `runwisp validate`. `runwisp import cron` explains each
  one.
- **An unreadable file is skipped; other files still load.** (For example: a
  spool file for a deleted user, wrong file mode.) It is a config error only when
  every matched file fails.
- **NSS/LDAP/SSSD users are not visible** to the default release build
  (`CGO_ENABLED=0`, reads only `/etc/passwd`), so their jobs are skipped. Check
  with `getent passwd <name>`. A CGO build, or adding the user to `/etc/passwd`,
  fixes it.
- **`MAILTO=` is reported, not used.** RunWisp keeps the output and notifies on
  failure instead. A [`sendmail` notifier](/notifications/providers/sendmail/)
  replaces cron mail and removes the warning. A `SHELL=` that isn't an absolute
  path is reported too.
- **Missed jobs are not run later.** A job missed while the daemon was down is
  recorded as missed, but not started. `promote` the task to get
  [`catch_up`](/configuration/tasks/#catch_up).
- **Overlapping runs queue.** Cron starts a second copy of a slow job; RunWisp
  waits for the first. `promote` the task and set
  [`max_concurrent`](/configuration/tasks/#max_concurrent) to change this.
- **Task names come from the command.** If two crontabs give the same name, the
  second becomes `backup-<crontab name>`, and the rename is reported.
- **Files anyone could edit are refused.** A group- or world-writable crontab,
  or one with an unrelated owner, fails the whole load. Spool directories are
  the exception: they are group-writable and sticky by design.
- **No `${...}` substitution** in cron files.
  `0 3 * * * dump.sh ${DB}` reaches the shell as written.
- **A file can't match both `include` and `include_cron`.** That is an error.
- **anacron is not read.** `/etc/anacrontab` jobs keep running under anacron.

Otherwise a cron task is a normal task: `[defaults]` applies, notifications
fire, `runwisp run` starts it, and reload treats it like any other task.

## CLI flags: config, data directory & listen address

These flags work on every `runwisp` subcommand (`daemon`, `tui`, `run`, and
others). A flag always beats its env var.

| Flag             | Default               | What it does                                                                                     |
| ---------------- | --------------------- | ------------------------------------------------------------------------------------------------ |
| `--config`, `-c` | `runwisp.toml`        | Config file path, relative to the working directory. Env: `RUNWISP_CONFIG`.                      |
| `--data`         | `.runwisp`            | Directory for all state: SQLite database, logs, PID file, socket. Env: `RUNWISP_DATA`.           |
| `--socket`       | `<data>/runwisp.sock` | Control socket path. See [Control socket](#control-socket). Env: `RUNWISP_SOCKET`.               |
| `--host`         | `127.0.0.1`           | HTTP bind address. `0.0.0.0` listens on every interface. Env: `RUNWISP_HOST`.                    |
| `--port`         | `9477`                | HTTP port (REST API, SSE, Web UI). Env: `RUNWISP_PORT`.                                          |
| `--log-level`    | `info`                | `debug`, `info`, `warn`, `error`. Env: `RUNWISP_LOG_LEVEL`. See [Logging](/operations/logging/). |
| `--log-format`   | `auto`                | `auto`, `text`, `json`. Env: `RUNWISP_LOG_FORMAT`.                                               |

```bash
runwisp daemon --data /var/lib/runwisp --host 0.0.0.0 --port 9477
```

- In a container, set the env vars rather than flags. The
  [official image](/getting-started/docker/#environment-defaults) uses them as
  defaults, and its healthcheck can't see a `--data` flag.
- To move the data directory, stop the daemon and move the directory.
- The password and JWT signing key are never stored. The password comes from
  `RUNWISP_PASSWORD`, or a new one is created at every boot. The JWT key is
  derived from the password, so a fixed `RUNWISP_PASSWORD` keeps browser
  sessions across restarts. See [Auth](/operations/auth/).
- All subcommands and flags: [CLI reference](/reference/cli/).

## Environment variables

| Variable                  | What it does                                                                                                                                                                           |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RUNWISP_PASSWORD`        | The daemon password (kept in memory only). When unset, a new password is created at every boot.                                                                                        |
| `RUNWISP_AUTH`            | `off` disables authentication (local dev or trusted networks only). Default `on`. Can't be combined with `RUNWISP_PASSWORD`. See [Auth](/operations/auth/#running-without-a-password). |
| `RUNWISP_TRUSTED_PROXIES` | Comma-separated CIDR list. Overrides [`trusted_proxies`](#trusted_proxies). Catch-all ranges are rejected.                                                                             |
| `RUNWISP_STATION_TOKEN`   | Used by `runwisp station` to connect to a control plane. Ignored in standalone mode.                                                                                                   |
| `RUNWISP_SOCKET`          | Same as `--socket`.                                                                                                                                                                    |
| `RUNWISP_CONFIG`          | Same as `--config`.                                                                                                                                                                    |
| `RUNWISP_DATA`            | Same as `--data`.                                                                                                                                                                      |
| `RUNWISP_HOST`            | Same as `--host`.                                                                                                                                                                      |
| `RUNWISP_PORT`            | Same as `--port`. An invalid value is a startup error.                                                                                                                                 |
| `RUNWISP_TLS`             | Overrides [`tls`](#tls-tls_cert-tls_key) (`auto` or `off`). Applied on every load, including reload, so it never blocks a [reload](/operations/reload/).                               |
| `RUNWISP_DEBUG_ADDR`      | Optional **loopback** address (for example `127.0.0.1:6060`) for Go `pprof` profiles. A non-loopback address is refused.                                                               |

## Control socket

The daemon creates a Unix socket, `<data>/runwisp.sock` by default. The CLI and
TUI use it without a password.

- Access is limited by the data directory's `0700` mode, the socket's `0600`
  mode, and a `SO_PEERCRED` check.
- The socket file is removed on a clean shutdown.
- `--socket` (or `RUNWISP_SOCKET`) moves it. The daemon and CLI must use the
  same path.
- Move it when the data directory is a bind mount that rejects `chmod` (Docker
  Desktop, some network filesystems). RunWisp warns and keeps working there, but
  a native path like `/run/runwisp.sock` avoids the warning.
- You can reach a daemon by its socket alone:
  `runwisp status --socket /run/runwisp.sock`.

---

<CardGrid>
    <LinkCard
        title="[tasks.*] ref"
        href="/configuration/tasks/"
        description="Per-task graceful_stop, which must fit inside shutdown_timeout."
    />
    <LinkCard
        title="[storage] ref"
        href="/configuration/storage/"
        description="Disk-usage limits for the data directory."
    />
</CardGrid>
