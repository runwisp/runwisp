---
# SPDX-FileCopyrightText: PoppyCake, s.r.o.
# SPDX-License-Identifier: Apache-2.0
title: "[tasks.*]"
description: Every key accepted by a [tasks.<name>] table — cron, run, on_overlap, retries, timeout, retention, notifications.
---

A task is a unit of work that runs and exits. The TOML key (`backup-db`
in `[tasks.backup-db]`) is the **task name** — it appears in the CLI,
the API, the Web UI, and the on-disk log path. Names must be unique
across both `[tasks.*]` and `[services.*]`.

Only two fields are truly required: the table itself and `run`.
Everything else inherits from [`[defaults]`](/configuration/defaults/)
or has a sensible built-in default.

## Minimum example

```toml
[tasks.heartbeat]
cron = "*/5 * * * *"
run  = "/usr/local/bin/heartbeat"
```

That's a complete, valid task: it fires every five minutes, captures
stdout/stderr to a log file, and persists each run to SQLite.

## Identity & metadata

| Key           | Type   | Default   | What it does                                                                                    |
| ------------- | ------ | --------- | ----------------------------------------------------------------------------------------------- |
| _table key_   | string | required  | The task name. Used in CLI (`runwisp exec <name>` / `runwisp run-task <name>`), API, log paths. |
| `run`         | string | required  | Shell command. Multi-line OK with TOML triple-quotes (`"""…"""`). Trusted input — see note.     |
| `description` | string | _(empty)_ | Human-readable description shown in the UI and TUI.                                             |
| `group`       | string | `"Tasks"` | UI grouping label. Set to share a section header with related tasks.                            |
| `api_trigger` | bool   | `true`    | Allow manual triggers from CLI / API / UI. Set `false` to make the task **cron-only**.          |

:::caution[`run` is trusted input]
`runwisp.toml` is read from disk, never accepted from the network. The
REST API and Web UI cannot mutate `run`. That said: the daemon executes
your command with whatever privileges it was started with. Don't run
the daemon as root unless you genuinely need it.
:::

## Scheduling

| Key                 | Type   | Default    | What it does                                                                                                                                                                            |
| ------------------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cron`              | string | _(none)_   | 5-field cron expression. Supports `@hourly`, `@every 1h30m`.                                                                                                                            |
| `timezone`          | string | inherited  | IANA timezone for this task's cron evaluation (e.g. `"America/New_York"`). Falls back to `[scheduler] timezone`, which is **required** when at least one cron task lacks a per-task TZ. |
| `catch_up`          | enum   | `"latest"` | What to do for missed firings on startup: `latest`, `all`, or `skip`.                                                                                                                   |
| `max_catch_up_runs` | int    | `100`      | Cap on catch-up runs when `catch_up = "all"` (`0` inherits, `-1` unlimited). See [scheduling page](/concepts/scheduling/#capping-the-backfill-max_catch_up_runs).                       |

Without a `cron`, the task is **manual-only** — it runs only when
triggered explicitly. That's a valid pattern for one-shot deploy hooks
and similar. **Manual-only is not the same as `catch_up = "skip"`**:
the former is unscheduled, the latter still fires on cron going
forward and just discards backfill — see the
[comparison](/concepts/scheduling/#manual-only-vs-catch_up--skip).

See [How scheduling works](/concepts/scheduling/) for the full cron
grammar and DST behaviour, and the catchup table.

### `api_trigger = false` — cron-only tasks

The default `api_trigger = true` lets you run the task on demand from
the CLI (`runwisp run-task <name>`), the REST API
(`POST /api/tasks/{name}/trigger`), the Web UI's **Run Task** button,
and the TUI's `r` keybinding.

Setting `api_trigger = false` says **only the scheduler may start this
task**. Use it for tasks that are dangerous to fire off-schedule —
overnight rebuilds, idempotency-fragile data jobs, anything that
coordinates with another schedule and would do harm if double-fired by
a curious operator.

```toml
[tasks.nightly-merge]
cron        = "0 4 * * *"
api_trigger = false        # only fires from cron, never from the UI / API / CLI
run         = "/usr/local/bin/merge-shards"
```

What changes when `api_trigger = false`:

- A `POST /api/tasks/{name}/trigger` returns **403 Forbidden** with the
  message `API triggering disabled for this task`.
- `runwisp run-task <name>` surfaces the same error and exits non-zero.
- The Web UI's **Run Task** button and the TUI's `r` action still appear
  on the task page; pressing them surfaces the daemon's 403 error
  inline. (The buttons are not currently greyed out — the daemon is the
  authority, not the client.)
- Cron firings, retries, and `on_overlap` behaviour are **unaffected**:
  the scheduler is not "API triggering" and runs the task normally.

`api_trigger` is independent of `cron`. A task with `api_trigger = false`
**and** no `cron` is a dead task — it can never start. The loader does
not reject this combination today, but it's almost certainly a mistake.

## Concurrency

| Key           | Type | Default   | What it does                                                                                 |
| ------------- | ---- | --------- | -------------------------------------------------------------------------------------------- |
| `parallelism` | int  | `1`       | Maximum concurrent runs of this task. Must be `> 0`.                                         |
| `on_overlap`  | enum | `"queue"` | What happens when a new firing arrives at the parallelism limit: `queue`/`skip`/`terminate`. |

Most cron-style work wants `parallelism = 1`. See
[Concurrency policies](/concepts/concurrency/) for when each policy is
right.

## Retries & timeout

| Key              | Type     | Default                                 | What it does                                                                                                                                |
| ---------------- | -------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `retry_attempts` | int      | `0`                                     | Additional attempts after the initial failure. Must be `>= 0`.                                                                              |
| `retry_delay`    | duration | **`5s` only when `retry_attempts > 0`** | Base delay between attempts. Conditional default: with retries off the field is unused; with retries on, an omitted value resolves to `5s`. |
| `retry_backoff`  | enum     | `"constant"`                            | Curve applied to `retry_delay`: `constant`, `linear`, or `exponential`.                                                                     |
| `timeout`        | duration | inherited from `[defaults]`             | Per-attempt wall-clock cap. `0` or unset means no timeout.                                                                                  |

A retry only fires for `failed` / `timeout` / `crashed`. Manual stops
and `on_overlap = "terminate"` end the chain — see the
[terminate ⇄ retries callout](/concepts/concurrency/#terminate). Full
table of formulas on the
[Retries & timeouts](/concepts/retries/) page.

`restart` may be set to `"never"` (default) or `"on_failure"`. Setting
`restart = "always"` on a task is **rejected** at config load — use
`[services.*]` for an always-on process.

## Logs & retention

| Key            | Type          | Default                     | What it does                                                                                                                                               |
| -------------- | ------------- | --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `log_max_size` | byte size     | `100MB`                     | Per-run log cap. Units: `b`, `kb`, `mb`, `gb`, `tb`. Use `"unlimited"` to opt out; bare `0` is rejected.                                                   |
| `log_on_full`  | enum          | `"drop_old"`                | What to do at the cap: `drop_new`, `drop_old`, `kill_task`.                                                                                                |
| `keep_runs`    | int / keyword | inherited from `[defaults]` | Keep the N most recent runs for this task. Positive `N` caps; `"unlimited"` opts out; omit to inherit. Bare `0` and negatives are rejected at config load. |
| `keep_for`     | duration      | inherited from `[defaults]` | Delete runs older than this. Accepts `d` and `w` units (`30d`, `2w`). `"unlimited"` opts out; omit to inherit. Zero and negatives are rejected.            |

Both `keep_runs` and `keep_for` apply if both are set; the stricter one
wins in practice. See [Logs & retention](/concepts/logs/) for the full
picture, including the `.prev` rotation lifecycle and the fixed
one-hour cleanup cadence.

## Notifications

| Key                 | Type     | Default  | What it does                                                           |
| ------------------- | -------- | -------- | ---------------------------------------------------------------------- |
| `notify_on_failure` | string[] | _(none)_ | Notifier IDs to alert on `run.failed` / `run.timeout` / `run.crashed`. |
| `notify_on_success` | string[] | _(none)_ | Notifier IDs to alert on `run.succeeded`.                              |

Each entry is a notifier `id` declared in a [`[[notifier]]`](/notifications/providers/overview/)
block. Whatever channels are listed in `[notify] append_notifiers`
(default: `["inapp"]`) are appended automatically — set
`append_notifiers = []` to opt out, or `["slack-ops"]` to make every
failure page Slack instead. See
[Notification sugar](/notifications/per-task/)
for how this expands.

These two fields are **symmetric across `[tasks.*]` and `[services.*]`** —
same field names, same expansion, same dedup rules. The
[Notification sugar](/notifications/per-task/) page covers both surfaces
and is the canonical reference; the
[`[services.*]` notifications](/configuration/services/#notifications) section
points back here.

## What's rejected on tasks

The config loader refuses these at startup so they can't fail silently
later:

- `restart = "always"` — use `[services.*]`.
- `instances = N` — services-only field.
- A task name that's also used by `[services.*]` — names share one namespace.
- Empty or missing `run`.

## Worked example

```toml
[tasks.process-event-queue]
group          = "Workers"
description    = "Drain the queue every 10 minutes with retries"
cron           = "*/10 * * * *"
on_overlap     = "skip"           # never two queue drainers at once
timeout        = "9m"             # die before the next firing
retry_attempts = 3
retry_delay    = "2s"
retry_backoff  = "exponential"
keep_runs      = 200
keep_for       = "14d"
notify_on_failure = ["slack-ops"]
run = """
set -eu
echo "Draining queue at $(date -Iseconds)"
/usr/local/bin/process-queue
"""
```

The full annotated example file lives at
[`apps/runwisp/runwisp.example.toml`](https://github.com/runwisp/runwisp/blob/main/apps/runwisp/runwisp.example.toml)
or you can drop a fresh copy with `runwisp init`.

## Where to next

- [`[services.*]` reference](/configuration/services/) — the always-on counterpart.
- [`[defaults]` reference](/configuration/defaults/) — what fields fall through.
- [Concurrency policies](/concepts/concurrency/), [Retries & timeouts](/concepts/retries/),
  [Logs & retention](/concepts/logs/) — the conceptual pages behind the keys.
