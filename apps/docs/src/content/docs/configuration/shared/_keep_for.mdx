**type**: `duration`

Delete runs older than this. With [`keep_runs`](/configuration/tasks/#keep_runs) also set, whichever
deletes more wins.

```toml
[tasks.backup-db]
keep_for = "90d"
```

Examples: `48h` (2 days), `14d`, `2w`, `90d`.
