**type**: `string`

A dotenv file merged below [`secrets`](/configuration/tasks/#secrets). Only the path is shown in the
API and UI, never the contents. Same paths and format as
[`env_file`](/configuration/tasks/#env_file).

```toml
[tasks.backup]
secrets_file = "/etc/runwisp/backup-secrets.env"
```
