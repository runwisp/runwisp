**type**: `string`

Path to a dotenv file merged beneath [secrets](/configuration/tasks/#secrets). Only
the path is ever visible in the API/UI, never the contents. Same path resolution
and format as [env_file](/configuration/tasks/#env_file).

```toml
[tasks.backup]
secrets_file = "/etc/runwisp/backup-secrets.env"
```
