---
# SPDX-FileCopyrightText: PoppyCake, s.r.o.
# SPDX-License-Identifier: Apache-2.0
title: Logs & retention
description: How RunWisp captures stdout/stderr, where logs live on disk, how rotation works, and how run history is bounded.
---

Every run's stdout and stderr is captured to its own log file under
the data directory. The database stores run metadata — exit code,
duration, timestamps — but **not** the log bodies. RunWisp isn't a log
aggregator. It's a log archive for "what did this task print on
2026-04-12 at 03:15?".

## Where logs live on disk

```
{data_dir}/logs/{task-name}/{YYYYMMDD}_{HHMMSS}_{run-id}.log
```

- **One file per run**, not one per task. Each run gets its own
  isolated log, named after its start time and ID so files are
  sortable and unique even when runs start less than a second apart.
- The daemon keeps tiny index files alongside each log so the Web UI
  can jump around long logs without re-reading them; you don't need to
  touch them.
- When a run is deleted by retention, the daemon removes the log file,
  its index files, and any parent directory that becomes empty.

## `log_max_size`

```toml
[tasks.bulky-job]
cron         = "0 4 * * *"
run          = "/usr/local/bin/bulky.sh"
log_max_size = "50MB"
```

- **Default**: `100MB`.
- **Scope**: per run. A task that runs hourly accumulates one fresh
  capped file per run, not one shared file growing forever.
- **Units**: `b`, `kb`, `mb`, `gb`, `tb` (case-insensitive). Bare
  numbers are parsed as bytes. `"100MB"`, `"1.5gb"`, `"4096"` are all
  valid.
- **`log_max_size = 0` means unbounded.** No cap is enforced, no lines
  are dropped. Use this only when you've thought about your disk.

## `log_on_full`

What happens when a run's output hits `log_max_size`:

| Value       | Behaviour                                                                                                                                                                                                                                                  |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `drop_old`  | Default. Rotates the current log to `.prev`, keeps writing fresh output. Earlier output lost.                                                                                                                                                              |
| `drop_new`  | Stops accepting new lines; the process keeps running. A system line records the truncation.                                                                                                                                                                |
| `kill_task` | Cancels the run's context, terminating the process. The run records as `log_overflow` (exit code from the kill) — a dedicated end reason that's still treated as a failure for retries and notifications, so the cause is visible without inspecting logs. |

Whichever policy fires, the daemon writes a synthetic line at the
truncation point so someone skimming the log can see exactly when the
limit hit. Nothing is dropped without a record.

`log_on_full` also controls what happens when `[storage] min_free_space`
trips during a run: `kill_task` cancels the run on disk pressure;
`drop_new` and `drop_old` quietly stop accepting lines (but the daemon
always raises a `log.disk_pressure` notification so the operator
discovers the dropped output). See
[storage configuration](/configuration/storage/#what-min_free_space-actually-does).

`drop_old` is the default because it preserves the **end** of the log —
which is usually where the failure is. `drop_new` is right when the
**start** is the interesting part (a daemon's startup banner, a long
batch's preamble) and the rest is repetitive noise.

## Retention: `keep_runs` and `keep_for`

Retention controls how long old run rows and their log files stick
around.

```toml
[tasks.metrics]
cron      = "* * * * *"
run       = "/usr/local/bin/metrics.sh"
keep_runs = 500
keep_for  = "7d"
```

- **`keep_runs`** — keep the N most recent runs for this task. Older
  runs are deleted (row + log files).
- **`keep_for`** — delete runs whose `created_at` is older than the
  given duration. Accepts extended units, including days and weeks:
  `"7d"`, `"2w"`, `"36h"`, `"30m"`.
- **Both at once**: both criteria contribute to deletion. A run is
  removed if **either** rule says so. The stricter rule wins in
  practice — set both if you want a hard floor _and_ a hard ceiling.
- **Per task**: each task's retention is evaluated independently using
  that task's own settings (or the inherited `[defaults]`).

Cleanup runs in the background — by default once an hour — so you may
see slightly more than `keep_runs` rows briefly between sweeps. That's
fine. Runs that are still going are never deleted.

When retention triggers, both the SQLite row **and** the log file (with
all its index files) are removed. There's no orphaning: a row never
points at a missing log, and a log file never lingers without a row.

## Live streaming

The Web UI tails logs in real time as the run produces them — new
lines appear within milliseconds. The stream first replays whatever's
already on disk for the run, then switches to live, so you don't miss
the start of the output.

If the producer pushes lines faster than your browser can keep up, the
oldest queued lines are dropped and the UI shows "N lines dropped"
so the gap is visible. The on-disk file always has the complete log;
the live stream is just a fast preview.

Idle browser connections refresh automatically every few minutes —
nothing for you to do.

## Crash safety

Logs are flushed and closed cleanly when a run ends or when the daemon
shuts down normally.

If the daemon is killed mid-write (power loss, force kill):

- The log file is **not truncated** — partial writes survive on disk,
  and the viewer tolerates a partial last line.
- On the next startup, the run is marked `crashed` (see
  [scheduling: on startup](/concepts/scheduling/#on-startup-unfinished-runs-are-marked-crashed))
  and its log file is left exactly as it was — those last lines are
  usually the most useful debugging artifact.

The result: every row in your run history finishes with a final
result, and every finished run has a log file you can read.

## Intentional scope

RunWisp captures logs for the runs it supervises. It is **not** a log
aggregator, ships nothing remotely, and does not match patterns
against captured output. If you need fleet-wide search or log-based
alerting, ship the per-run files (or your task's stdout) to a tool
that's built for it — Loki, ELK, CloudWatch, etc. If you want "alert
when stderr says ERROR", grep inside your task and exit non-zero;
RunWisp will fire its normal failure notification.

## Where to next

- [Notifications model](/notifications/model/) — how a failed run
  ends up in your inbox or chat.
- [How scheduling works](/concepts/scheduling/) — what creates the run
  rows that retention later trims.
- [`[tasks.*]` reference](/configuration/tasks/) — `log_max_size`,
  `log_on_full`, `keep_runs`, `keep_for`.
