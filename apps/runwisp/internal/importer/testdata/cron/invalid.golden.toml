# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[tasks.bad]
description = "Broken nightly job"
cron = "99 99 * * *"  # TODO: RunWisp couldn't parse this cron expression — fix it.
run = "/bin/bad"
