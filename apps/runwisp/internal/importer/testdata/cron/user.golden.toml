# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[tasks.backup]
description = "Nightly full backup"
cron = "30 2 * * *"
timezone = "Europe/Bratislava"
shell = "/bin/bash"
env_base = "clean"
working_dir = "~"
run = "/usr/local/bin/backup.sh --full"

[tasks.backup.env]
PATH = "/usr/local/bin:/usr/bin:/bin"

[tasks.logrotate]
description = "Rotate logs every Sunday"
cron = "0 0 * * 0"
timezone = "Europe/Bratislava"
shell = "/bin/bash"
env_base = "clean"
working_dir = "~"
run = "/usr/sbin/logrotate /etc/logrotate.conf"

[tasks.logrotate.env]
PATH = "/usr/local/bin:/usr/bin:/bin"

[tasks.cleanup]
cron = "@daily"
timezone = "Europe/Bratislava"
shell = "/bin/bash"
env_base = "clean"
working_dir = "~"
run = "/opt/app/cleanup"

[tasks.cleanup.env]
PATH = "/usr/local/bin:/usr/bin:/bin"

# @reboot — runs once each time the daemon starts.
[tasks.warmup]
run_on_start = true
timezone = "Europe/Bratislava"
shell = "/bin/bash"
env_base = "clean"
working_dir = "~"
run = "/opt/app/warmup"

[tasks.warmup.env]
PATH = "/usr/local/bin:/usr/bin:/bin"

[tasks.sync]
description = "Heavy job, deprioritized"
cron = "*/15 * * * *"
timezone = "Europe/Bratislava"
shell = "/bin/bash"
env_base = "clean"
working_dir = "~"
run = "/usr/bin/nice -n 19 /opt/sync.sh"

[tasks.sync.env]
PATH = "/usr/local/bin:/usr/bin:/bin"
