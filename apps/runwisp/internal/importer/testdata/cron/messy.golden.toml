# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[tasks.job]
description = "Two jobs that derive the same name → deduped"
cron = "0 1 * * *"
run = "/bin/job"

[tasks.job-2]
cron = "0 2 * * *"
run = "/bin/job"

[tasks.printf]
description = "A percent sign means stdin to crond; RunWisp passes it to the shell verbatim."
cron = "0 3 * * *"
run = "/usr/bin/printf 'hello%world'"
