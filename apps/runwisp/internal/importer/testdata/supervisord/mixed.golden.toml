# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[services.api]
group = "site"
run = "/srv/api/bin/serve --port 8080"
working_dir = "/srv/api"

[tasks.batch]
group = "site"
run = "/srv/api/bin/migrate"
run_on_start = true
restart = "never"
