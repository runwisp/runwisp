# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[services.api]
run = "/srv/api/bin/serve"
working_dir = "/srv/api"
user = "api"

[services.cache]
run = "/usr/bin/redis-server /etc/redis/redis.conf"
stop_signal = "SIGTERM"
