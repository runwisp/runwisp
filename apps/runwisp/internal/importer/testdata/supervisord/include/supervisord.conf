; Top-level config that pulls program definitions out of conf.d/, the way a
; real /etc/supervisor/supervisord.conf does.

[supervisord]
logfile=/var/log/supervisor/supervisord.log

[include]
files=conf.d/*.conf
