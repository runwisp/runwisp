# Generated by `runwisp import`. Review the notes below, then
# validate with `runwisp validate`.

[tasks.oneshot]
description = "Database migration"
run = "/usr/local/bin/migrate up"
run_on_start = true
user = "deploy"

[tasks.oneshot.env]
DATABASE_URL = "postgres://localhost/app"
