# Copyright (c) PoppyCake, s.r.o. SPDX-License-Identifier: Apache-2.0
$schema: 'https://moonrepo.dev/schemas/workspace.json'

# Unscoped targets (`moon run dev`, `moon run demo`) resolve to the runwisp
# app directly, so passthrough args (`moon run dev -- <args>`) reach the binary.
defaultProject: 'runwisp'

# Explicit map (not globs): apps/ui and packages/ui would both derive the id "ui".
projects:
  root: '.'
  runwisp: 'apps/runwisp'
  web-ui: 'apps/ui'
  docs: 'apps/docs'
  ui: 'packages/ui'
  common: 'packages/common'
  asyncapi: 'packages/asyncapi'
  eslint-config: 'packages/eslint-config'
  typescript-config: 'packages/typescript-config'

vcs:
  defaultBranch: 'main'

pipeline:
  # `bun install` is run explicitly; moon never installs.
  installDependencies: false
